# Entorno virtual (se recrea con: python -m venv venv + pip install -r requirements.txt)
venv/

# Archivos temporales de Python
__pycache__/
*.pyc

# Base de datos local de SQLite (este proyecto usa MySQL, pero por si acaso)
db.sqlite3
